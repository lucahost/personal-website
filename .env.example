# Endpoint for the terminal easter-egg telemetry (the rate-limited VPS proxy that
# forwards to SigNoz). Leave empty to disable telemetry (e.g. local dev).
# Example: VITE_TRACK_ENDPOINT=https://hostettler.io/track
VITE_TRACK_ENDPOINT=
